Add freelists for :class:`int` objects with up to five internal digits (about 150 bits), and allocate the exact result size in most multi-digit additions and multiplications so the results can be reused from those freelists.
